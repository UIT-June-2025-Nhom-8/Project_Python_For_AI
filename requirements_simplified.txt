# Simplified Requirements for Sentiment Analysis Project
# Only essential packages needed for clean implementation

# Core machine learning
scikit-learn>=1.3.0
pandas>=1.5.0
numpy>=1.21.0

# Optional: for better performance and additional features
matplotlib>=3.5.0  # for visualizations (optional)
seaborn>=0.11.0   # for better plots (optional)
wordcloud>=1.9.0  # for word clouds (optional)

# Development and jupyter notebooks (optional)
jupyter>=1.0.0
ipykernel>=6.0.0

# For saving models
pickle-protocol>=5  # usually included with Python
