{
  "config_info": {
    "name": "Accuracy Optimized Configuration",
    "description": "Tối ưu hóa cho độ chính xác cao nhất với Amazon Reviews 500k samples",
    "target_dataset_size": 1000000,
    "expected_training_time": "2-3 hours",
    "memory_requirement": "8GB RAM",
    "priority": "Accuracy > Speed"
  },
  
  "dataset_config": {
    "train_size": 200000,
    "test_size": 100000,
    "validation_split": 0.1,
    "random_state": 42
  },
  
  "logistic_regression": {
    "model_params": {
      "C": 1.5,
      "max_iter": 2000,
      "solver": "liblinear",
      "class_weight": "balanced",
      "random_state": 42
    },
    "tfidf_params": {
      "max_features": 40000,
      "ngram_range": [1, 3],
      "min_df": 3,
      "max_df": 0.88,
      "sublinear_tf": true,
      "stop_words": null,
      "lowercase": true,
      "strip_accents": "unicode"
    },
    "training_params": {
      "test_size": 0.15,
      "stratify": true
    }
  },
  
  "random_forest": {
    "model_params": {
      "n_estimators": 300,
      "max_depth": 30,
      "min_samples_split": 3,
      "min_samples_leaf": 1,
      "max_features": "sqrt",
      "bootstrap": true,
      "oob_score": true,
      "class_weight": "balanced",
      "random_state": 42,
      "n_jobs": -1
    },
    "tfidf_params": {
      "max_features": 15000,
      "ngram_range": [1, 3],
      "min_df": 3,
      "max_df": 0.88,
      "sublinear_tf": true,
      "stop_words": null,
      "lowercase": true,
      "strip_accents": "unicode"
    },
    "training_params": {
      "test_size": 0.15,
      "stratify": true
    }
  },
  
  "gradient_boosting": {
    "model_params": {
      "n_estimators": 300,
      "learning_rate": 0.08,
      "max_depth": 6,
      "subsample": 0.85,
      "max_features": "sqrt",
      "validation_fraction": 0.1,
      "n_iter_no_change": 15,
      "tol": 1e-4,
      "random_state": 42
    },
    "tfidf_params": {
      "max_features": 10000,
      "ngram_range": [1, 3],
      "min_df": 3,
      "max_df": 0.88,
      "sublinear_tf": true,
      "stop_words": null,
      "lowercase": true,
      "strip_accents": "unicode"
    },
    "training_params": {
      "test_size": 0.15,
      "stratify": true
    }
  },
  
  "optimization_notes": {
    "logistic_regression": [
      "Tăng C=1.5 để giảm regularization cho accuracy cao hơn",
      "Tăng max_iter=2000 để đảm bảo convergence",
      "max_features=20000 để capture nhiều sentiment patterns",
      "class_weight='balanced' để handle class imbalance"
    ],
    "random_forest": [
      "Tăng n_estimators=250 cho ensemble effect tốt hơn",
      "max_depth=30 cho deep trees capture complex patterns",
      "min_samples_split=3 để allow detailed splits",
      "oob_score=true để validation không cần thêm data"
    ],
    "gradient_boosting": [
      "n_estimators=300 với early stopping để tránh overfitting",
      "learning_rate=0.08 cho stable learning",
      "max_depth=6 để capture non-linear sentiment patterns",
      "subsample=0.85 cho stochastic gradient boosting"
    ]
  }
}
