{
  "config_info": {
    "name": "Balanced Configuration",
    "description": "Cân bằng giữa độ chính xác và performance, data có thể lên tới 500k samples",
    "target_dataset_size": 100000,
    "expected_training_time": "1-1.5 hours",
    "memory_requirement": "8GB RAM",
    "priority": "Balance Accuracy & Speed"
  },
  
  "dataset_config": {
    "train_size": 100000,
    "test_size": 10000,
    "random_state": 42
  },
  
  "logistic_regression": {
    "model_params": {
      "C": 1.0,
      "max_iter": 2000,
      "solver": "liblinear",
      "class_weight": "balanced",
      "random_state": 42
    },
    "tfidf_params": {
      "max_features": 30000,
      "ngram_range": [1, 2],
      "min_df": 5,
      "max_df": 0.85,
      "sublinear_tf": true,
      "stop_words": null,
      "lowercase": true,
      "strip_accents": "unicode"
    },
    "training_params": {
      "test_size": 0.2,
      "stratify": true
    }
  },
  
  "random_forest": {
    "model_params": {
      "n_estimators": 200,
      "max_depth": 20,
      "min_samples_split": 5,
      "min_samples_leaf": 2,
      "max_features": "sqrt",
      "bootstrap": true,
      "class_weight": "balanced",
      "random_state": 42,
      "n_jobs": -1
    },
    "tfidf_params": {
      "max_features": 15000,
      "ngram_range": [1, 2],
      "min_df": 5,
      "max_df": 0.85,
      "sublinear_tf": true,
      "stop_words": null,
      "lowercase": true,
      "strip_accents": "unicode"
    },
    "training_params": {
      "test_size": 0.2,
      "stratify": true
    }
  },
  
  "gradient_boosting": {
    "model_params": {
      "n_estimators": 200,
      "learning_rate": 0.1,
      "max_depth": 4,
      "subsample": 0.8,
      "max_features": "sqrt",
      "validation_fraction": 0.1,
      "n_iter_no_change": 10,
      "tol": 1e-4,
      "random_state": 42
    },
    "tfidf_params": {
      "max_features": 5000,
      "ngram_range": [1, 2],
      "min_df": 5,
      "max_df": 0.85,
      "sublinear_tf": true,
      "stop_words": null,
      "lowercase": true,
      "strip_accents": "unicode"
    },
    "training_params": {
      "test_size": 0.2,
      "stratify": true
    }
  },
  
  "optimization_notes": {
    "logistic_regression": [
      "C=1.0 cân bằng regularization cho good generalization",
      "max_features=15000 đủ cho sentiment patterns nhưng không quá memory intensive",
      "ngram_range=(1,2) để tối ưu tốc độ nhưng vẫn capture context",
      "min_df=5 để loại bỏ rare words, giảm noise"
    ],
    "random_forest": [
      "n_estimators=150 cân bằng ensemble effect và training time",
      "max_depth=20 tránh overfitting với big data",
      "min_samples_split=5 để prevent overfitting",
      "max_features='sqrt' optimal cho text classification"
    ],
    "gradient_boosting": [
      "n_estimators=150 với early stopping cho optimal performance",
      "learning_rate=0.1 cân bằng convergence speed và stability",
      "max_depth=4 đủ cho capturing patterns nhưng tránh overfitting",
      "subsample=0.8 cho regularization effect"
    ]
  },
  
  "performance_expectations": {
    "logistic_regression": {
      "expected_accuracy": "0.85-0.88",
      "training_time_minutes": "10-15",
      "memory_usage_gb": "2-3"
    },
    "random_forest": {
      "expected_accuracy": "0.86-0.89",
      "training_time_minutes": "25-35",
      "memory_usage_gb": "3-4"
    },
    "gradient_boosting": {
      "expected_accuracy": "0.87-0.90",
      "training_time_minutes": "35-50",
      "memory_usage_gb": "2-3"
    }
  }
}
